rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Universal open access for festival registrations, ticketing, dossiers, and settings
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
